@model Tema2_MiniMagazin.Models.Produse

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Produse</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.NumeProdus)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NumeProdus)
            @Html.ValidationMessageFor(model => model.NumeProdus)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Pret)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Pret)
            @Html.ValidationMessageFor(model => model.Pret)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
